/**
 * Point d’entrée typé pour les formes partagées côté API moteur (F.82).
 * Préférer `import type { … } from '@/lib/types'` pour les nouveaux fichiers.
 */

export type { EngineCountryListRow } from './engine-country-list-row';
export type {
  ProbabilityApiBreakdown,
  ProbabilityApiRow,
  RecommendationApiBreakdown,
  RecommendationApiItem,
  RecommendationLevel,
} from './api-recommendation-probability';
