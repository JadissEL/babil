node_modules
# Keep environment variables out of version control
.env
.env.local
.next
dev.db
.agent-state
.cursor_memory
tsconfig.tsbuildinfo

/generated/prisma

.vercel

# Optional URL map for agent manifest fetches (may contain internal endpoints)
data/agent-manifest-url-map.json
